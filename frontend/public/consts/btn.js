import CustomButton from "../object/CustomButton";

const continuar = scene.add.existing(new CustomButton(scene, x/2 , y/2 + 50, 'button1','button2', 'button3', 'Continuar', 'Tutorial').setScale(ratio));
        

export {
    continuar
}

